extends layout

block content
  .jumbotron.jumbotron-fluid
    .container
      h1.display-4 Ex2
      p.lead
        | Seja Bem-Vindo à página de termos índices!!
  table.table.table-striped
    thead
      tr
        th(scope='col') Termo
        th(scope='col') Id Classe
    tbody
      each d in data
        tr(id=d.termo)
          th(scope='row') 
            a(href='#' + d.termo) #{d.termo} 
          td 
            a(href='/classes/' + d.idClasse) #{d.idClasse} 
  p(style='text-align: center; padding-top: 10px;') 
    a.btn.btn-secondary(href='/') Voltar à Página Inicial
    a.btn.btn-info(href='javascript:history.back()') Anterior
